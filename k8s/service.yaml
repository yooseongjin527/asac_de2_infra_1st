apiVersion: v1
kind: Service
metadata:
  name: raffle-service-v1
  namespace: default
spec:
  selector:
    app: raffle
    version: v1
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: raffle-service-v2
  namespace: default
spec:
  selector:
    app: raffle
    version: v2
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer